function Car(name, milage, max_speed) {
    this.name = name;
    this.milage = milage;
    this.max_speed = max_speed;
}

let lembo = new Car("Lamborgini", "7.69kml", "350km/h");

console.log(lembo);